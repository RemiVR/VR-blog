<h1><%= @user.name%></h1>
<h1><%= @user.email%></h1>
<h2><%= @user.id%></h2>
	<% if can? :update, @user %>
		<%= link_to "edit", edit_user_path %>
	<% end %>
	<% if can? :delete, @user%>
		<%= link_to "Delete", user_path, data: {confirm: "are you sure?"}, method: :delete %>
	<% end %>


